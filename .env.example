# Database Configuration (local development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=books_user
DB_PASSWORD=books_password
DB_NAME=books_db

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_ENVIRONMENT=development

# Optional: set to 1 to run tests with SQLite
USE_SQLITE_FOR_TESTS=0
